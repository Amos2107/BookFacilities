<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Rental Facilities App</title>
    </head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Rental Facilities</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link" href="/addFacilities">Add Facility</a></li>
            <li class="nav-item"><a class="nav-link" href="/admin/users">Manage Users</a></li>
            <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>
 
    <div class="container mt-5">
      <div class = "text-center mb-4">
        <h2>Update Facility</h2>
      </div>
       
      <div class="text-center">
          <% if (facilities) { %>
            <div class="card shadow mx-auto" style="max-width: 700px;">
              <div class="card-body">
                <form action="/editFacilities/<%= facilities.facilities_id %>" method="POST" enctype="multipart/form-data">
 
                <div class="mb-3">
                <label for="name" class="form-label">Facility Name:</label>
                <input type="text" id="name" name="name" class="form-control" value = "<%= facilities.name %>" required>
                </div>
 
                <div class="mb-3">
                <label for="description" class="form-label">Facility Description:</label>
                <input type="text" id="description" name="description" class="form-control" value = <%= facilities.description %> required>
                </div>
               
                <div class="mb-3">
                <label for="location" class="form-label">Facility Location:</label>
                <input type="text" id="location" name="location" class="form-control" value = <%= facilities.location %> required>
                </div>
               
                <div class="mb-3">
                <label for="capacity" class="form-label">Facility Capacity:</label>
                <input type="number" id="capacity" name="capacity" min="0" step="1" class="form-control" value = <%= facilities.capacity %> required>
                </div>
 
                <div class="mb-3">
                <label for="facilities_group" class="form-label">Facility Group:</label>
                <select
                <input class="form-select" id="facilities_group" name="facilities_group" value = <%= facilities.facilities_group %> required>
                <option value="" disabled selected>Select a group</option>
                <option value="Entertainment">Entertainment</option>
                <option value="Meeting">Meeting</option>
                <option value="Gym">Gym</option>
                <option value="Rest">Rest</option></select>
                </div>
 
                <div class="mb-3">
                <label for="image" class="form-label">Current Image:</label>
                <input type="text" name="currentImage" class="form-control" value="<%= facilities.image %>" readonly>
                <img src = "/images/<%= facilities.image %>" width="20%">
                </div>
 
                <div class="mb-3">
                  <label for="image" class="form-label">Upload New Image</label>
                  <input type="file" id="image" name="image" class="form-control" class="form-control" accept="image/*">
                  </div>
 
                <div class="d-grid gap-2">
                  <button type="submit" class="btn btn-primary">Update Facility</button>
                  <a href="/" class="btn btn-secondary">Back</a>
                </div>
            </form>
          <% } else { %>
            <div class="alert alert-warning text-center">No facility found.</div>
            <div class="text-center">
          <% } %>
        </div>
    </div>
</body>
</html>