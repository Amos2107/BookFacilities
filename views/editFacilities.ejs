<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Rental Facilities App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Rental Facilities</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addFacilities">Add new Facilities</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Facility</h2></div>
        <br>
        <div class="text-center">
          <% if (facilities) { %> 
            <form action="/editFacilities/<%= facilities.facilities_id %>" method="POST" enctype="multipart/form-data">
                <label for="name">Facility Name:</label><br>
                <input type="text" id="name" name="name" value = "<%= facilities.name %>" required><br><br>

                <label for="description">Facility Description:</label><br>
                <input type="text" id="description" name="description" value = <%= facilities.description %> required><br><br>

                <label for="location">Facility Location:</label><br>
                <input type="text" id="location" name="location" value = <%= facilities.location %> required><br><br>

                <label for="capacity">Facility Capacity:</label><br>
                <input type="number" id="capacity" name="capacity" min="0" step="1" value = <%= facilities.capacity %> required><br><br>

                <label for="facilities_group">Facility Group:</label><br>
                <input type="text" id="facilities_group" name="facilities_group" value = <%= facilities.facilities_group %> required><br><br>

                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value="<%= facilities.image %>" readonly><br>
                <img src = "/images/<%= facilities.image %>" width="20%"><br><br>

                New Image:<br><input type="file" id="image" name="image" accept="image/*" value="<%= facilities.image %>"><br><br>

                <button type="submit">Update Facility</button>
            </form>
          <% } else { %>
              <p>No facility found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
