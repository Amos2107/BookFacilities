<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Rental Facilities App</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Rental Facilities</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <% if (user && user.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/addFacilities">Add Facility</a></li>
            <li class="nav-item"><a class="nav-link" href="/admin/users">Manage Users</a></li>
          <% } %>
          <% if (user) { %>
            <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
            <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
 
    <div class="container">
        <br>
        <div class="text-center"><h2>Facility Details</h2></div>
        <br>
            <% if (facilities) { %> <!-- Check if facility data exists -->
              <div class="card shadow mx-auto" style="max-width: 600px;">
                <img src="/images/<%= facilities.image %>" class="card-img-top" alt="Facility Image">
                <div class="card-body">
                  <h5 class="card-title"><%= facilities.name %></h5>
                  <p class="card-text"><strong>Description:</strong> <%= facilities.description %></p>
                  <p class="card-text"><strong>Location:</strong> <%= facilities.location %></p>
                  <p class="card-text"><strong>Capacity:</strong> <%= facilities.capacity %> pax</p>
                  <p class="card-text"><strong>Group:</strong> <%= facilities.facilities_group %></p>
                </div>
        <% } else { %>
            <div class="alert alert-warning text-center">
              No Facility found.
            </div>
        <% } %>
        <div class="text-center">
        <a href="/" class="btn btn-secondary">Back</a>
      </div>
   
</body>
</html>